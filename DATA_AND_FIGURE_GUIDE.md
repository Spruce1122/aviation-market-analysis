# 1 使用说明

上传 `.xlsx` 后，网页先校验字段、国家—年份主键、年份格式和非负性，再计算指标。左侧依次进入“数据概览”“全样本分析”“分国家ASK/RPK分析”“出发侧（ASK_out）与到达侧（ASK_in）分析”。每个图表页面提供当前数据、PNG/PDF/CSV或XLSX下载；数据概览可生成本次完整结果ZIP。

年份控件依据对应航空指标的实际有效范围生成。`Data!Time`可能包含宏观变量覆盖年份，并不代表ASK/RPK在同一时期均有数据。缺失年份不插值，0值保留并按各指标规则判断是否有效。

# 2 原始数据

## 2.1 Data

- `Data!Country Name`：国家名称。
- `Data!Country Code`：三位国家代码。
- `Data!Time`：整数年份。
- `Data!ASKs`：可用座公里，表示运力供给。
- `Data!RPKs`：收入客公里，表示实际实现的客运运输量。

## 2.2 country_year_ask_capacity

- `country_year_ask_capacity!country_code`：三位国家代码。
- `country_year_ask_capacity!country_name`：国家名称。
- `country_year_ask_capacity!year`：整数年份。
- `country_year_ask_capacity!ASK_out`：从该国机场出发的国际航段运力。
- `country_year_ask_capacity!ASK_in`：抵达该国机场的国际航段运力。

# 3 核心指标

- `ASK_growth_it = ASK_it / ASK_i,t-1 - 1`。仅在年份连续且前后两期ASK均大于0时有效。
- `RPK_growth_it = RPK_it / RPK_i,t-1 - 1`。仅在年份连续且前后两期RPK均大于0时有效。
- `common_growth_sample`：同一国家—年份的ASK_growth和RPK_growth均有效。
- `growth_gap = RPK_growth - ASK_growth`；正值表示RPK增长相对领先，负值表示ASK增长相对领先。
- `abs_growth_gap = |growth_gap|`。
- `PLF = RPK / ASK × 100%`；图中仅将 `0 < PLF ≤ 100%` 视为有效PLF。
- `market_size_group`：按全样本期国家平均ASK排序，前40国为Large、后40国为Small，其余合格国家为Medium。
- `ASK_index`、`RPK_index`：在所选区间内，以各国首个ASK与RPK共同为正年份为共同基期，各自除以基期值并乘100。
- `R = mean(ASK_out) / mean(ASK_in)`；`ln_R = ln(R)`。均值仅使用所选时期ASK_out和ASK_in同时非缺失的年份。

# 4 全样本分析

## 4.1 ASK/RPK年度增长率趋势（F02）

1. **回答的问题**：全球样本中运力供给ASK与实际客运量RPK的年度变化是否同步。
2. **原始数据**：`World_Development_Indicators.xlsx`；Sheet `Data`；字段 `Data!Country Name`、`Data!Country Code`、`Data!Time`、`Data!ASKs`、`Data!RPKs`。
3. **计算**：按连续正值年份分别计算ASK_growth和RPK_growth，再按年份取跨国中位数。两项同比独立计算。
4. **横轴**：年份。
5. **纵轴**：各年跨国同比增长率中位数，单位%。
6. **点、线**：蓝线为ASK，橙线为RPK；0线区分增长与下降。
7. **怎么读**：两线接近表示总体同步；下降阶段RPK低于ASK表示需求收缩快于运力撤减；恢复阶段RPK高于ASK表示需求恢复快于运力投放。
8. **注意事项**：ASK与RPK年度有效国家数可能不同；图中中位数不是全球加总量增速。

## 4.2 ASK与RPK年度增长同步与偏离（F01）

1. **回答的问题**：共同同比样本中ASK与RPK的增长方向和幅度是否一致。
2. **原始数据**：`Data!Country Name`、`Data!Country Code`、`Data!Time`、`Data!ASKs`、`Data!RPKs`。
3. **计算**：筛选common_growth_sample，并将ASK_growth与RPK_growth乘100。
4. **横轴**：ASK年度增长率（%）。
5. **纵轴**：RPK年度增长率（%）。
6. **点、颜色、参考线**：每个点是一条国家—年份观测；颜色为Large/Medium/Small；虚线为 `y=x`。
7. **怎么读**：线上方RPK增长更快，线下方ASK增长更快；右上同时增长，左下同时下降，右下和左上表示方向相反；离线越远，幅度偏离越大。
8. **注意事项**：增长偏离只能描述供需变化关系，运力是否过剩还需结合PLF、绝对规模和市场背景。

## 4.3 不同规模市场ASK/RPK增长趋势（F03）

1. **回答的问题**：不同市场规模国家的冲击幅度、恢复速度和ASK/RPK协调程度是否不同。
2. **原始数据**：`Data!Country Name`、`Data!Country Code`、`Data!Time`、`Data!ASKs`、`Data!RPKs`。
3. **计算**：固定Large/Medium/Small分组，组内按年份分别计算ASK_growth和RPK_growth中位数。
4. **横轴**：年份。
5. **纵轴**：组内同比增长率中位数（%）。
6. **面板和线**：三个面板依次为Large、Medium、Small；蓝线ASK，橙线RPK。
7. **怎么读**：比较两线距离、下降幅度、恢复速度和组间波动。某组两线波动更大，可描述该组ASK/RPK调整更不稳定。
8. **注意事项**：分组按全样本期平均ASK固定；图形不能单独识别组间差异的原因。

## 4.4 不同规模市场ASK/RPK同步性与偏离（F04）

1. **回答的问题**：三个规模组在共同同比样本中的供需调整协调性。
2. **原始数据**：`Data!Country Name`、`Data!Country Code`、`Data!Time`、`Data!ASKs`、`Data!RPKs`。
3. **计算**：各组计算ASK/RPK增长Pearson相关系数、`mean(|RPK_growth-ASK_growth|)×100`和严格反方向观测占比。
4. **横轴**：Large、Medium、Small。
5. **纵轴**：A面板为相关系数；B为平均绝对增长差（百分点）；C为反方向比例（%）。
6. **柱和标注**：颜色表示规模组；柱顶数值为指标值和共同同比观测数N。
7. **怎么读**：高相关、小绝对差和低反方向比例共同表示较强协调性；低相关、大绝对差和高反方向比例表示偏离更明显。
8. **注意事项**：相关性不表示因果关系；平均指标可能掩盖个别年份和国家的极端变化。

读图示例：若某组相关系数0.85、平均绝对增长差8个百分点、反方向比例6%，通常可描述为该组整体同步性较强。以上数字仅说明读图方法，不代表当前网页实际结果。

## 4.5 PLF年度变化与市场规模比较（A02）

1. **回答的问题**：三个自选时期的运力利用程度如何变化，不同规模组表现是否一致。
2. **原始数据**：`Data!Country Name`、`Data!Country Code`、`Data!Time`、`Data!ASKs`、`Data!RPKs`。
3. **计算**：`PLF=RPK/ASK×100%`；仅保留 `0<PLF≤100%`。第一时期为多年时每国至少2个有效观测并取中位数，单年时至少1个；两个比较年份均须有效，三个时期使用同一批国家。
4. **横轴**：第一时期、第二时期年份、第三时期年份。
5. **纵轴**：PLF（%）。
6. **线和区间**：左图为全样本中位数及25%—75%分位区间；右图为三个规模组中位数。
7. **怎么读**：PLF提高表示已投放运力转化为实际客运量的比例改善；下降表示客运量相对运力减弱。
8. **注意事项**：PLF是相对利用程度；高PLF不能直接说明绝对客运规模更大。

## 4.6 ASK/RPK年度增长关系分时期观察（A03）

1. **回答的问题**：三个时期的ASK/RPK增长关系是否发生结构变化。
2. **原始数据**：`Data!Country Name`、`Data!Country Code`、`Data!Time`、`Data!ASKs`、`Data!RPKs`。
3. **计算**：各时期独立筛选common_growth_sample；第一时期可包含多年，第二和第三时期为单年。
4. **横轴**：ASK年度增长率（%）。
5. **纵轴**：RPK年度增长率（%）。
6. **点和参考线**：每点为国家—年份；三个面板共用坐标范围；虚线为 `y=x`。
7. **怎么读**：观察点云相对45°线的位置、离散程度和象限变化，判断各时期需求相对运力的收缩或恢复速度。
8. **注意事项**：各时期国家样本可能不同；高增长率可能包含低基数效应。

# 5 分国家ASK/RPK分析

## 5.1 ASK/RPK国家排名与排名查询（T01）

1. **回答的问题**：各国运力与实际客运规模在所选时期处于什么位置。
2. **原始数据**：`Data!Country Name`、`Data!Country Code`、`Data!Time`、`Data!ASKs`、`Data!RPKs`。
3. **计算**：筛选ASK、RPK同时为正的年份；单年用当年值，多年取共同有效年份均值；加权PLF=`ΣRPK/ΣASK×100%`。
4. **各列**：国家、ISO、有效年份、ASK、RPK、ASK排名、RPK排名、加权PLF及增长率中位数。
5. **排序**：根据用户选择的ASK或RPK降序，ISO用于稳定处理同值。
6. **交互**：Top N允许1至当前合格国家总数；国家查询显示ASK排名、RPK排名、PLF和有效年份。
7. **怎么读**：ASK排名反映运力供给规模，RPK排名反映实际客运规模；ASK排名明显靠前可描述为运力规模相对实际客运规模更加靠前。
8. **注意事项**：排名差不能直接认定运力过剩，需结合PLF、趋势和市场背景。

## 5.2 自选国家ASK/RPK规模与指数比较（A01）

1. **回答的问题**：自选国家的航空市场绝对规模和相对自身基期的变化轨迹。
2. **原始数据**：`Data!Country Name`、`Data!Country Code`、`Data!Time`、`Data!ASKs`、`Data!RPKs`。
3. **计算**：绝对模式直接使用ASKs/RPKs；指数模式以所选区间内每国首个ASK与RPK共同正值年份为100，后续值除以各自基期值乘100。
4. **横轴**：年份。
5. **纵轴**：绝对模式为座公里；指数模式为基期=100的指数。
6. **线和面板**：每国一个面板；蓝线ASK，橙线RPK；指数图100虚线为共同基期。
7. **怎么读**：绝对模式比较市场规模；指数模式比较变化速度和相对轨迹。
8. **注意事项**：指数200与150只说明前者相对自身基期增长更快，不能说明其绝对规模更大。

## 5.3 国家ASK/RPK动态关系类型（F06）

1. **回答的问题**：各国长期ASK/RPK增长同步性、偏离幅度和动态类型。
2. **原始数据**：`Data!Country Name`、`Data!Country Code`、`Data!Time`、`Data!ASKs`、`Data!RPKs`。
3. **计算**：所选时期按国计算增长相关系数、增长差中位数、平均绝对增长差、反方向比例、增长差标准差和有效年份数；合格国家四分位数用于动态分类。
4. **横轴**：ASK增长与RPK增长的Pearson相关系数。
5. **纵轴**：平均绝对增长差（百分点）。
6. **点、颜色、大小**：每点一个国家；颜色为动态类型；面积表示有效年份数；用户选择国家加黑色外圈并优先标注。
7. **怎么读**：右下表示相关高且偏离小，左上表示相关低且偏离大。
8. **注意事项**：分类阈值由当前时期合格样本动态形成；数据或时间更新后国家类型可以改变。

## 5.4 各国ASK/RPK年度变化方向统计（T02）

1. **回答的问题**：所选时期国家—年份观测中，同向增长、同向下降和方向偏离各占多少。
2. **原始数据**：`Data!Country Name`、`Data!Country Code`、`Data!Time`、`Data!ASKs`、`Data!RPKs`。
3. **计算**：仅使用common_growth_sample，按两项增长率的严格正负号划分四个互斥类别，再计算同方向与反方向小计。
4. **各列**：变化方向、观测数、占有效样本比例；明细表另含国家、年份和两项增长率。
5. **统计单位**：国家—年份观测。
6. **类别**：同时增长、同时下降、ASK增长/RPK下降、ASK下降/RPK增长、同方向合计、反方向合计。
7. **怎么读**：前两类表示共同扩张或收缩；后两类表示供需变化方向偏离。
8. **注意事项**：增长率恰为0的观测保留在有效样本分母中，但不进入严格增减类别。

# 6 出发侧（ASK_out）与到达侧（ASK_in）分析

## 6.1 ASK_out / ASK_in国家规模排名（T04）

1. **回答的问题**：各国出发侧或到达侧国际航空运力的绝对规模排名。
2. **原始数据**：`country_year_ask_capacity!country_code`、`country_name`、`year`、`ASK_out`、`ASK_in`。
3. **计算**：只使用ASK_out和ASK_in同时非缺失年份；单年用当年值，多年取所选期均值。
4. **各列**：国家、ISO、平均ASK_out、平均ASK_in、R、ln(R)、有效年份数。
5. **排序**：按用户选择的ASK_out或ASK_in降序。
6. **交互**：Top N允许1至当前合格国家总数。
7. **怎么读**：ASK_out排名描述出发侧绝对国际运力；ASK_in排名描述到达侧绝对国际运力。
8. **注意事项**：绝对规模排名与F07的方向不对称程度属于两类指标。

## 6.2 ASK_out与ASK_in方向不对称（F07）

1. **回答的问题**：一个国家国际航段的出发侧与到达侧运力是否对称。
2. **原始数据**：`country_year_ask_capacity!country_code`、`country_name`、`year`、`ASK_out`、`ASK_in`。
3. **计算**：多年 `R=mean(ASK_out)/mean(ASK_in)`，单年为当年ASK_out/ASK_in，`ln_R=ln(R)`。
4. **横轴**：ln(R)。
5. **纵轴**：国家。
6. **柱和参考线**：0线为两侧对称；负值为ASK_in相对更高，正值为ASK_out相对更高；标签显示R。
7. **怎么读**：绝对值越大，方向结构偏离越明显；留空国家选择时自动显示ln(R)两端极端国家。
8. **注意事项**：R衡量座公里运力供给，`R>1`不能解释为该国是旅客净输出国。

## 6.3 ASK_out / ASK_in极端不对称国家（T03）

1. **回答的问题**：哪些国家的出发侧或到达侧结构偏向最明显。
2. **原始数据**：`country_year_ask_capacity!country_code`、`country_name`、`year`、`ASK_out`、`ASK_in`。
3. **计算**：与F07相同地计算R和ln(R)，再分别取ln(R)最低和最高的N国。
4. **各列**：国家、ISO、平均ASK_out、平均ASK_in、R、ln(R)、有效年份数。
5. **排序**：最低组由小到大，最高组由大到小。
6. **分组**：ln(R)最低组为ASK_in相对占优，最高组为ASK_out相对占优。
7. **怎么读**：用于快速识别方向结构偏向最明显的国家，并与F07图形相互核对。
8. **注意事项**：极端名单随时间区间和上传数据变化；T03与F07始终共用同一统计口径。

# 7 数据更新后如何理解结果变化

数据更新后会自动变化：年度增长率、国家排名、Top N名单、散点位置、F06四分位分类阈值和国家类型、ASK_out/in排名、极端方向国家、PLF时期结果。

固定方法设定：Large前40、Small后40、连续正值同比规则、R和ln(R)公式、国家指数共同基期定义。侧栏修改Large/Small数量后，新的参数会明确显示并用于当前会话。

新增航空指标年份时，各图的有效时间范围自动扩展。只增加宏观变量而没有增加ASK/RPK时，航空图表时间范围保持原有有效覆盖。结果变化应先检查新增Excel数据和样本筛选，再解释经济含义；程序不会为匹配历史数字而修改数据。
